# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-dotenv==1.0.1

# Qdrant vector database client
qdrant-client==1.11.3

# Embedding model (CPU-only, no torch required)
fastembed>=0.3.6

# sentence-transformers for rebuild_index.py
sentence-transformers>=3.1.0

# LLM inference via Groq
groq>=0.9.0

# HTTP client (used by web_search & affidavit fetcher)
httpx==0.27.2

# HTML parsing (affidavit scraper)
beautifulsoup4==4.12.3
lxml>=5.0.0

# Playwright (JS-rendered page scraping in rebuild_index.py)
playwright>=1.44.0

# Rate Limiter
slowapi==0.1.9
